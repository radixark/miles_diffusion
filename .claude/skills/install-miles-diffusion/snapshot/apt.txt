# apt packages install.sh installs on a bare CUDA 12.9 Ubuntu 24.04 box.
# Versions stay unpinned: the image is Ubuntu 24.04 too, so the same release pockets
# hand out the same versions.

# --- interpreter + build toolchain -------------------------------------------
python3
python3-dev
python3-pip
python3-venv
build-essential
git
git-lfs
curl
ca-certificates

# --- python dists the official image gets from apt, not pip ------------------
# Listed in env/not-locked.txt. Installing them from apt (rather than letting pip
# pull newer ones from PyPI) is what keeps cryptography/oauthlib/pyparsing/... at
# the exact versions the official image reports.
python3-apt
python3-blinker
python3-cryptography
python3-dbus
python3-distro
python3-gi
python3-httplib2
python3-launchpadlib
python3-lazr.restfulclient
python3-lazr.uri
python3-oauthlib
python3-pyparsing
python3-wadllib

# --- runtime shared libraries -------------------------------------------------
# opencv / paddleocr need libGL + glib; ray and the rollout engines need numa;
# sglang's transfer engines dlopen the rdma verbs providers.
libglib2.0-0t64
libgl1
libsm6
libxext6
libxrender1
libnuma1
numactl
libibverbs1
librdmacm1t64
ibverbs-providers
libsndfile1
